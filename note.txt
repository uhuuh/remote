- 删除mcp前端
- 将所有代码放在一个文件中
- 该文件只做两件事，初始化连接，同步代码和执行任务
- 连接同样支持多种后端和相关参数
- 同步代码
-- 增加一个参数是否需要同步，和在远程后端上的代码地址
-- 然后使用git导出patch，在远程代码仓库中应用patch，注意要排除当前文件
-- 如果patch应用失败，应该抛出异常
- 执行任务，可以给定多个任务，每个对应有一个名字和一个执行命令字符串
-- 务必注意，任务执行输出应该实时返回
- 上面三个阶段可以设置为一个内嵌的config对象，嵌入在文件中

使用 superpower skill


