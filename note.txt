设计一个mcp服务器
- 要求支持ssh、docker、wsl三个后端
- 支持两个基本接口
- 第一个接口在首次调用要求提供选择的后端和相应初始化信息，用户提供应该将该信息保存在本地，下次打印新session可以确认是否直接服用上次连接信息
- 第二个接口输入任意的字符串在相应后端上执行
-- 注意应该在同一个交互shell中，这意味着不同的执行可以相互影响，比如前一个操作创建的环境变量，后面操作可以直接读取
-- 注意命令的输出应该实时返回

使用superpower skill实现

